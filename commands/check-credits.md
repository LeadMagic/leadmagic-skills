---
description: Check LeadMagic credit balance and plan headroom (free)
---

Call LeadMagic MCP `check_credit_balance` (or `GET /v1/credits` with `X-API-Key`). Summarize remaining credits, plan notes if present, and — using the plan ladder in the `api-auth-credits` skill — what that balance can afford (e.g. emails found, validations, mobile numbers). Free call; no confirmation needed.
